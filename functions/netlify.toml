# Configuración de reseteo: Simple y explícita.
# Netlify usará esto porque borramos la configuración de la UI.
[build]
  functions = "functions/"
  publish = "."

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true

