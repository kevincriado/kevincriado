# Configuración de compilación mínima y explícita
[build]
  functions = "functions/"
  publish = "."

# Regla de redirección forzada y directa
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
  force = true
